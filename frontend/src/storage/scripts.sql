CREATE TABLE events (
    post_id: INTEGER
    user_id: INTEGER
    user_name: VARCHAR(255)
    title: VARCHAR(255)
    content: VARCHAR(255)
    address: VARCHAR(255)
    latitude: DECIMAL
    longitude: DECIMAL
    created_at: VARCHAR(255)
    attendees: INTEGER
    deadline: VARCHAR(255)
)